REMOTE_FRONTEND_PORT=5174
VITE_REMOTE_API_BASE_URL=http://localhost:3002

# Optional: only for client-side 413 fallback text if server body is missing; should match BACKUP_IMPORT_MAX_BYTES on remote-backend.
# VITE_BACKUP_IMPORT_MAX_BYTES=52428800
